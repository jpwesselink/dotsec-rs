[package]
name = "dotenv"
version = "6.0.0"
edition = "2021"
description = "Parser and validator for dotsec .env/.sec files"
license = "MIT"

[dependencies]
indexmap = "2"
pest = { workspace = true }
pest_derive = "2.1"
regex = "1"
serde_json = "1.0"
zeroize = "1.8"
